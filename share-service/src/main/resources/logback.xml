<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- 控制台输出的 Appender -->
    <appender name="stdout" class="ch.qos.logback.core.ConsoleAppender">
        <Target>System.out</Target>
        <encoder>
            <charset>UTF-8</charset>  <!-- 正确的编码配置 -->
            <pattern>%d{yyyy-MM-dd_HH:mm:ss.SSS} %5p [%t] [%c{1}]:%L - %m%n</pattern>
        </encoder>
    </appender>

    <!-- 文件输出的 Appender（按时间滚动） -->
    <appender name="logfile" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <encoder>
            <charset>UTF-8</charset>  <!-- 正确的编码配置 -->
            <pattern>%d %p [%t] [%c]:%L - %m%n</pattern>
        </encoder>
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>DEBUG</level>
        </filter>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>log/%d{yyyy-MM-dd-HH}'.log'</fileNamePattern>
        </rollingPolicy>
    </appender>

    <!-- 各包的日志级别配置 -->
    <logger name="org.springframework" level="WARN" />
    <logger name="org.springframework.remoting" level="WARN" />
    <logger name="org.springframework.scheduling.quartz" level="WARN" />
    <logger name="org.springframework.data.jpa" level="DEBUG" />
    <logger name="org.cometd" level="WARN" />
    <logger name="ch.qos.logback" level="WARN" />
    <logger name="com.springapp.mvc" level="DEBUG" />
    <logger name="com.cy.share.mapper" level="DEBUG"/>
    <logger name="com.cy.share" level="INFO"/>
    <logger name="com.xxl.job" level="INFO"/>

    <logger name="com.ibatis" level="DEBUG"></logger>

    <!-- 根日志配置 -->
    <root level="ERROR">
        <appender-ref ref="stdout" />
        <appender-ref ref="logfile" />
    </root>
</configuration>