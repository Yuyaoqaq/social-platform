<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cy.share.mapper.LikeRecordMapper">

    <insert id="insertIgnore">
        INSERT IGNORE INTO like_record (user_id, log_id, create_time)
        VALUES (#{userId}, #{logId}, #{createTime})
    </insert>

    <delete id="deleteByUserAndLog">
        DELETE FROM like_record WHERE user_id = #{userId} AND log_id = #{logId}
    </delete>

    <select id="countByUserIdAndLogId" resultType="int">
        SELECT COUNT(1) FROM like_record
        WHERE user_id = #{userId} AND log_id = #{logId}
    </select>

    <select id="selectByLogId" resultType="com.cy.share.pojo.LikeRecord">
        SELECT user_id, log_id, create_time FROM like_record
        WHERE log_id = #{logId}
        ORDER BY create_time DESC
    </select>

    <select id="selectByUserId" resultType="com.cy.share.pojo.LikeRecord">
        SELECT user_id, log_id, create_time FROM like_record
        WHERE user_id = #{userId}
        ORDER BY create_time DESC
    </select>

</mapper>
